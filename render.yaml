services:
  - type: web
    name: apollo-mission-control
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn --app-dir src apollo_mission_control.web_app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
